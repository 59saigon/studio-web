import { BaseQuery, GetAllQuery, GetByIdQuery } from "../BaseQuery";

export interface WeddingGetByIdQuery extends GetByIdQuery{
    
}

export interface WeddingGetAllQuery extends GetAllQuery{
    
}